/* Your task is to display to the user the key and key code they press.

Example of output: You've pressed the "a" key. It's key code is 65.

Wes Bos made this https://keycode.info/ for us to use as an example. */
var output = document.getElementById('output')

window.addEventListener('keydown',keyPress, false)

function keyPress (key) {
    if(key.keyCode == "65"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "81"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "90"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "87"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "83"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "88"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "69"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "68"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "67"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "82"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "70"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "67"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "82"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "70"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "86"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "84"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "71"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "66"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "89"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "72"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "78"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "85"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "74"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "77"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "73"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "75"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "79"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
    if(key.keyCode == "80"){
        output.innerHTML = `You've pressed the 'a' key. its Key code is:${key.keyCode}`
    }
}